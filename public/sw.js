/**
 * CRIS Golf Program — Service Worker (subphase 1F)
 *
 * Strategy: "App Shell + Static Precache"
 *   - On install: precache the app shell (HTML pages, JS/CSS chunks, icons,
 *     manifest) so the app opens offline after the first visit.
 *   - On fetch: cache-first for precached assets; network-first for navigation
 *     requests (so a live deploy always wins over stale HTML once online).
 *
 * Privacy (§8): This service worker makes NO third-party network calls.
 *   It does NOT use push notifications, background sync, or analytics.
 *   It only caches first-party assets from this same origin.
 *   youtube-nocookie embeds are NOT precached — they load from the network
 *   only when the user presses play (as in 1E), keeping the "no third-party
 *   contact on page load" guarantee intact.
 *
 * Update / versioning:
 *   Bump CACHE_VERSION below on every deploy that changes the precache list.
 *   The install event caches under the new key; the activate event deletes
 *   the old cache.  skipWaiting + clients.claim mean the new SW takes effect
 *   immediately for all open tabs — no "waiting for the next navigation"
 *   limbo.  Users testing updates should open an Incognito window or use
 *   DevTools > Application > Service Workers > "Update on reload".
 *
 * How to add/remove precached pages:
 *   1. Update PRECACHE_URLS below.
 *   2. Bump CACHE_VERSION.
 *   3. Deploy. The new SW will install and replace the old cache on activate.
 */

// ─── Cache versioning ────────────────────────────────────────────────────────
// Bump this string on every deploy to invalidate the old cache.
const CACHE_VERSION = "v4"; // bumped: added iOS splash images (1G follow-up)
const CACHE_NAME = `cris-golf-${CACHE_VERSION}`;

// ─── URLs to precache ────────────────────────────────────────────────────────
// All locale-prefixed routes (4 locales × 6 screens + root redirect).
// Static-export pages land at these exact paths in the out/ directory.
const PRECACHE_URLS = [
  // Root locale-redirect page
  "/",
  // English
  "/en",
  "/en/introduction",
  "/en/admissions",
  "/en/tuition",
  "/en/gallery",
  "/en/faq",
  "/en/privacy",
  // Korean
  "/ko",
  "/ko/introduction",
  "/ko/admissions",
  "/ko/tuition",
  "/ko/gallery",
  "/ko/faq",
  "/ko/privacy",
  // Chinese (Simplified)
  "/zh",
  "/zh/introduction",
  "/zh/admissions",
  "/zh/tuition",
  "/zh/gallery",
  "/zh/faq",
  "/zh/privacy",
  // Thai
  "/th",
  "/th/introduction",
  "/th/admissions",
  "/th/tuition",
  "/th/gallery",
  "/th/faq",
  "/th/privacy",
  // App icons (referenced by manifest + apple-touch-icon)
  "/icons/icon-192.png",
  "/icons/icon-512.png",
  "/icons/icon-512-maskable.png",
  "/icons/apple-touch-icon.png",
  // Web manifest
  "/manifest.webmanifest",
  // iOS startup images — generated by scripts/generate-splash.mjs (1G follow-up)
  // One PNG per device resolution × orientation; served via apple-touch-startup-image
  // <link> tags in app/layout.tsx.  Precaching ensures the splash shows instantly
  // when the installed PWA is launched offline.
  "/icons/splash/splash-iphone-se1-portrait.png",
  "/icons/splash/splash-iphone-se1-landscape.png",
  "/icons/splash/splash-iphone-8-portrait.png",
  "/icons/splash/splash-iphone-8-landscape.png",
  "/icons/splash/splash-iphone-8-plus-portrait.png",
  "/icons/splash/splash-iphone-8-plus-landscape.png",
  "/icons/splash/splash-iphone-x-portrait.png",
  "/icons/splash/splash-iphone-x-landscape.png",
  "/icons/splash/splash-iphone-xs-max-portrait.png",
  "/icons/splash/splash-iphone-xs-max-landscape.png",
  "/icons/splash/splash-iphone-xr-portrait.png",
  "/icons/splash/splash-iphone-xr-landscape.png",
  "/icons/splash/splash-iphone-12-portrait.png",
  "/icons/splash/splash-iphone-12-landscape.png",
  "/icons/splash/splash-iphone-12-max-portrait.png",
  "/icons/splash/splash-iphone-12-max-landscape.png",
  "/icons/splash/splash-iphone-14-pro-portrait.png",
  "/icons/splash/splash-iphone-14-pro-landscape.png",
  "/icons/splash/splash-iphone-14-pro-max-portrait.png",
  "/icons/splash/splash-iphone-14-pro-max-landscape.png",
  "/icons/splash/splash-iphone-15-pro-max-portrait.png",
  "/icons/splash/splash-iphone-15-pro-max-landscape.png",
  "/icons/splash/splash-ipad-97-portrait.png",
  "/icons/splash/splash-ipad-97-landscape.png",
  "/icons/splash/splash-ipad-10-5-portrait.png",
  "/icons/splash/splash-ipad-10-5-landscape.png",
  "/icons/splash/splash-ipad-11-portrait.png",
  "/icons/splash/splash-ipad-11-landscape.png",
  "/icons/splash/splash-ipad-12-9-portrait.png",
  "/icons/splash/splash-ipad-12-9-landscape.png",
  "/icons/splash/splash-ipad-10th-portrait.png",
  "/icons/splash/splash-ipad-10th-landscape.png",
  "/icons/splash/splash-ipad-mini-6-portrait.png",
  "/icons/splash/splash-ipad-mini-6-landscape.png",
];

// ─── Install: precache all shell assets ──────────────────────────────────────
self.addEventListener("install", (event) => {
  event.waitUntil(
    caches
      .open(CACHE_NAME)
      .then((cache) => {
        // Cache each URL INDEPENDENTLY (not cache.addAll, which is atomic and
        // would discard the entire batch if any single URL 404s). This keeps
        // the precache resilient: one missing route never empties the cache.
        return Promise.allSettled(
          PRECACHE_URLS.map((u) =>
            cache.add(u).catch((err) => {
              console.warn("[SW] Precache failed for", u, err);
            })
          )
        );
      })
      .then(() => {
        // Skip the waiting phase so the new SW activates immediately.
        return self.skipWaiting();
      })
  );
});

// ─── Activate: delete stale caches ───────────────────────────────────────────
self.addEventListener("activate", (event) => {
  event.waitUntil(
    caches
      .keys()
      .then((keys) =>
        Promise.all(
          keys
            .filter((key) => key.startsWith("cris-golf-") && key !== CACHE_NAME)
            .map((key) => {
              console.log("[SW] Deleting stale cache:", key);
              return caches.delete(key);
            })
        )
      )
      .then(() => {
        // Claim all open clients immediately so the new cache is used at once.
        return self.clients.claim();
      })
  );
});

// ─── Fetch: serve from cache, fall back to network ───────────────────────────
self.addEventListener("fetch", (event) => {
  const { request } = event;
  const url = new URL(request.url);

  // Only handle same-origin requests — never intercept third-party calls
  // (youtube-nocookie, fonts, etc.) to respect §8.
  if (url.origin !== self.location.origin) {
    return; // let the browser handle it normally
  }

  // Navigation requests (HTML pages): network-first so a fresh deploy is
  // served immediately when online; fall back to cache when offline.
  if (request.mode === "navigate") {
    event.respondWith(
      fetch(request)
        .then((networkResponse) => {
          // Cache the fresh page for offline use
          if (networkResponse.ok) {
            caches.open(CACHE_NAME).then((cache) => {
              cache.put(request, networkResponse.clone());
            });
          }
          return networkResponse;
        })
        .catch(async () => {
          // Offline: serve the exact cached page if we have it.
          const cached = await caches.match(request);
          if (cached) return cached;

          // Otherwise fall back to the requested locale's root so an offline
          // user keeps their own language (e.g. /th for an uncached /th/gallery),
          // then English, then the bare root redirect. Each match is awaited
          // individually because caches.match() returns a Promise (which is
          // always truthy) — a plain `||` chain would never reach later options.
          const localeRoot = "/" + (url.pathname.split("/")[1] || "en");
          return (
            (await caches.match(localeRoot)) ||
            (await caches.match("/en")) ||
            (await caches.match("/"))
          );
        })
    );
    return;
  }

  // Static assets (JS, CSS, images, fonts): cache-first for speed.
  event.respondWith(
    caches.match(request).then((cached) => {
      if (cached) return cached;

      // Not in cache — fetch from network and cache for next time.
      return fetch(request).then((networkResponse) => {
        if (networkResponse.ok) {
          caches.open(CACHE_NAME).then((cache) => {
            cache.put(request, networkResponse.clone());
          });
        }
        return networkResponse;
      });
    })
  );
});
